Restaurant logo (Flutter web)

Place your logo file here with this exact name:

  restaurant_logo.png

After `flutter build web`, this folder is copied to build/web/branding/.
You can also replace restaurant_logo.png on the server in the same path next to index.html
without rebuilding the app.

Use PNG (transparency supported). If the file is missing, the app shows a built-in icon.
